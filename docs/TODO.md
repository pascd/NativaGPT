* Add error handling for failed LLM or TTS responses
* Add a volume indicator while listening
* Add different wake word responses for variety
* Add the ability to interrupt the TTS response
* Add conversation history
* Add a way to adjust the listening timeout duration on the fly